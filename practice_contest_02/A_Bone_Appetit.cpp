#include <bits/stdc++.h>
using namespace std;
void solve()
{
    int n, m, x, y;
    cin >> n >> m;
    cin >> x >> y;
    cout << (n * x + m * y) << '\n';
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);

    solve();
    return 0;
}